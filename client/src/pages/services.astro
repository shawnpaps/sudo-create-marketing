---
import BaseLayout from "../layouts/BaseLayout.astro";
import ServicesPage from "../components/react/ServicesPage.tsx";

const SITE_URL = "https://sudocreate.studio";

const structuredData = {
	"@context": "https://schema.org",
	"@type": "WebPage",
	name: "Services | Sudo Create",
	description: "From visual media production and post to content-driven websites and media infrastructure — Sudo Create handles the full chain for brands where media is the product.",
	url: `${SITE_URL}/services`,
	publisher: { "@id": `${SITE_URL}/#organization` },
};
---

<BaseLayout
	title="Services | Sudo Create"
	description="From visual media production and post to content-driven websites and media infrastructure — Sudo Create handles the full chain for brands where media is the product."
	structuredData={structuredData}
>
	<div class="fixed top-0 left-0 w-full h-full -z-10">
		<img
			class="object-cover w-full h-full"
			src="/lens-background.jpg"
			alt=""
		/>
	</div>

	<ServicesPage client:load />
</BaseLayout>
